{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "api-sync",
      "type": "shell",
      "command": "uv sync",
      "options": {
        "cwd": "${workspaceFolder}/api"
      },
      "presentation": {
        "reveal": "silent"
      }
    },
    {
      "label": "web-install",
      "type": "shell",
      "command": "npm install",
      "options": {
        "cwd": "${workspaceFolder}/web"
      },
      "presentation": {
        "reveal": "silent"
      }
    }
  ]
}
